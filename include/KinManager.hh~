#ifndef KinManager_h
#define KinManager_h 1

#include <string>

using namespace std;

class KinManager {
  public:
  
  KinManager(double m_a, double m_A);
  ~KinManager();

  void SetEbeam(double E_a);
  double GetTheta(double theta_cm);
  double GetPhi(double theta_cm);
  double GetE_b(double theta_cm);
  double GetE_B(double theta_cm);

  private:

  double E_b, change, gamma, s, theta, theta_cm, E_B;

};

#endif
